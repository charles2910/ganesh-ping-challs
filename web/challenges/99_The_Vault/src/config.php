<?php
    /**
     * Caso o PHP não encontra a classe SQL Lite é necessário instalar o módulo 
     * utilizando:
     * # sudo apt-get install php7.x-sqlite3
     * 
     * onde x é a versão atual do php da sua máquina verificado em:
     * # php --version
     * 
     * Não é necessário configurar mais nada, uma vez que o db está no arquivo já 
     * dentro da própria pasta.
     */

    // Define a flag
    $FLAG = "Ganesh{w3lc0m3_t0_th3_v4u1t}";

    // Definindo o arquivo de banco
    $database_file = "62eada04c9def1816f3fa30817273e90.db";
